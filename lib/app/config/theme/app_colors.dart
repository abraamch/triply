import 'package:flutter/material.dart';

/// Paleta de colores oficial de Triply:
/// Estética premium, limpia, viajera y moderna.
class AppColors {
  // Fondos y Superficies
  static const Color background = Color(0xFF0F172A);      // Navy Dark profundo
  static const Color surface = Color(0xFF1E293B);         // Slate oscuro para Cards
  static const Color surfaceLight = Color(0xFF334155);    // Slate medio para bordes/divisores
  static const Color backgroundLight = Color(0xFFF8FAFC); // Blanco ahumado para modo claro
  static const Color cardLight = Color(0xFFFFFFFF);       // Blanco puro

  // Acentos de Marca
  static const Color primary = Color(0xFF0EA5E9);        // Turquesa cielo vibrante
  static const Color primaryLight = Color(0xFF38BDF8);   // Turquesa claro
  static const Color secondary = Color(0xFF14B8A6);      // Teal esmeralda viajero
  static const Color accent = Color(0xFFF59E0B);         // Ámbar sol / estrellas

  // Textos
  static const Color textPrimary = Color(0xFFF8FAFC);    // Blanco casi puro
  static const Color textSecondary = Color(0xFF94A3B8);  // Gris claro slate
  static const Color textMuted = Color(0xFF64748B);      // Gris apagado

  static const Color textDark = Color(0xFF0F172A);       // Para botones claros
  static const Color textDarkSecondary = Color(0xFF475569);

  // Estados
  static const Color success = Color(0xFF10B981);        // Verde esmeralda
  static const Color warning = Color(0xFFF59E0B);        // Ámbar
  static const Color error = Color(0xFFEF4444);          // Rojo coral suave
  static const Color info = Color(0xFF0EA5E9);           // Celeste info
}
